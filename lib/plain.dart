import 'package:flutter/material.dart';

class Plain extends StatelessWidget {
  const Plain({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Plain Widget'));
  }
}
